# scikit-learn is pinned to a minor range on purpose: the .joblib files in
# models/ are pickles built by 1.8, and loading them under a different minor
# version either warns or breaks outright. Bump this and retrain together.
scikit-learn>=1.8,<1.9

streamlit>=1.49      # dataframes default to full width from here on
pandas>=2.0
joblib>=1.3

# numpy is deliberately absent — it arrives via pandas/scikit-learn and nothing
# here imports it directly.
